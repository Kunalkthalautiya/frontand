# TaskFlow Frontend (Phase 1 - Auth)

1. Install deps:
   npm install

2. Run dev:
   npm run dev

3. Open:
   http://localhost:3000

4. Configure backend:
   set NEXT_PUBLIC_API_URL in .env.local (default http://localhost:8080)
